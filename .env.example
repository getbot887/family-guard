# KidSafe 部署配置
# 复制为 .env 并修改实际值

# 数据库
DB_NAME=family_guard
DB_USER=postgres
DB_PASSWORD=your-db-password

# JWT密钥（必须修改！用 openssl rand -base64 32 生成一个随机串）
JWT_SECRET=change-this-to-a-random-32-char-string

# CORS允许来源（生产环境填你的域名，如 https://app.familyguard.com）
CORS_ORIGIN=*
